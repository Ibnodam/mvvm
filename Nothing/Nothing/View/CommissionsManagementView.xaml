<Window x:Class="Nothing.View.CommissionsManagementView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление комиссиями" Height="500" Width="800"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Управление комиссиями" 
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Левая часть: Список комиссий -->
        <GroupBox Grid.Row="1" Grid.Column="0" Header="Комиссии" Margin="0,0,5,0">
            <DockPanel>
                <!-- Кнопки управления списком -->
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,5">
                    <Button Content="Добавить" Command="{Binding AddCommissionCommand}" Margin="0,0,5,0"/>
                    <Button Content="Удалить" Command="{Binding DeleteCommissionCommand}"/>
                </StackPanel>

                <!-- Список комиссий -->
                <ListBox ItemsSource="{Binding Commissions}" SelectedItem="{Binding SelectedCommission}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical" Margin="5">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding ChairmanName, StringFormat='Председатель: {0}'}" 
                                           Foreground="Green" FontSize="10"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </GroupBox>

        <!-- Правая часть: Редактирование комиссии -->
        <GroupBox Grid.Row="1" Grid.Column="1" Header="Редактирование комиссии" Margin="5,0,0,0">
            <StackPanel>
                <!-- Название комиссии -->
                <TextBlock Text="Название комиссии:"/>
                <TextBox Text="{Binding SelectedCommission.Name}" Margin="0,0,0,10"/>

                <!-- Описание комиссии -->
                <TextBlock Text="Описание комиссии:"/>
                <TextBox Text="{Binding SelectedCommission.Profile}" 
                         TextWrapping="Wrap" Height="60" Margin="0,0,0,10"/>

                <!-- Председатель -->
                <TextBlock Text="Председатель:"/>
                <ComboBox ItemsSource="{Binding AvailableMembers}" 
                          SelectedItem="{Binding SelectedChairman}"
                          DisplayMemberPath="Name" Margin="0,0,0,10"/>

                <!-- Кнопка сохранения -->
                <Button Content="Сохранить изменения" 
                        Command="{Binding SaveCommissionCommand}"
                        HorizontalAlignment="Right" Width="150"/>
            </StackPanel>
        </GroupBox>

        <!-- Кнопки управления -->
        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Закрыть" Width="100" Margin="5" 
                    Command="{Binding CloseCommand}" 
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
        </StackPanel>
    </Grid>
</Window>